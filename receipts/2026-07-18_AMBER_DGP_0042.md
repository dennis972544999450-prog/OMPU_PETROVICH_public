# Amber DGP-0042 Receipt

Date: 2026-07-18
Status: completed, postflight verified, provenance scar recorded

## Scope

This is the public, secret-free receipt for one Den-authorized substitute turn
of the dual-gardener bus-to-Infograph route.

- pass: `dgp-0042-petrovich-codex-for-phi-hausmaster-amber-4101675ec0`
- canonical slot: `phi-hausmaster`
- executor: `petrovich-codex`
- compiler: `petrovich-codex-gardener-amber`
- compiler model line: `gpt-5.6-sol`
- execution mode: `substitute`
- team: `amber`

The slot, executor, and compiler identities remain distinct. This pass did not
impersonate Phi or turn temporary support into response debt.

## Candidate Result

The compiler read all 200 review targets and all 200 fresh targets in order.
Both packets contain historical Bolt TTL wrappers rather than the original
message bodies.

- review: 3 blocks, 2 edges, one `zusammenfassung`, zero orphan blocks;
- fresh: 11 blocks, 10 edges, one `zusammenfassung`, zero orphan blocks;
- combined intents: 26, below the hard cap of 32;
- exact execution binding: matched;
- base validator: green for both candidates;
- connectivity verifier: green for both candidates.

Canonical candidate hashes:

- review: `f0277cc42e4fb58ad25b8a4d82c4ab8995c4edb5bd8ffa8950fb710a34a59357`
- fresh: `1c6ddaf011f2030ad74e6cbe49d4cd365d74a8d2c0c6539e3ef1cb4365f96adb`

The review retains one narrow rule: TTL cooling closes an inactive
notification, not the meaning or correction branches behind it. The fresh
candidate preserves a May 29-June 11 arc of failure, minimal-crew preservation,
pause, guarded rebuilding, explicit release states, memory-companion practice,
and later technical and attribution corrections. All mutable claims remain
historical testimony rather than current-state assertions.

## Promotion Proof

The copy dry-run saw the exact 26 intent IDs:

- applied: 26
- skipped: 0
- rejected: 0
- deferred: 0
- integrity: `ok`
- foreign-key violations: 0
- live DB mutated: false

The live promotion then used the same intent set:

- graph before: 1630 blocks, 2711 edges, 361 applied-intent ledger rows;
- graph after: 1644 blocks, 2723 edges, 387 applied-intent ledger rows;
- applied: 26;
- rejected/deferred: 0/0;
- postflight integrity: `ok`;
- postflight foreign-key violations: 0;
- pass ready queue: 0;
- canonical ready queue: 0.

An online SQLite backup was created before the live drain. Its SHA-256 is
`7f41b08d583df22fe6ac118eafef1de662485904e1f50b011050028a7212c37c`.

## Baton Proof

- coverage after: 2800 historical source messages;
- retrospective messages remaining: 2673;
- state revision after completion: 44;
- active pass after completion: none;
- next agent: `petrovich-codex`;
- Phi executor statistics: unchanged.

This receipt proves one bounded route from historical bus wrappers to an
attributed graph delta. It does not claim that the source projects or public
endpoints named inside those wrappers are currently live.

## Provenance Scar

The compiling architect first validated different candidate shapes, then
observed both working candidate files replaced by the current 3+2 and 11+10
shapes. The exact process that performed the replacement was no longer
recoverable after the pass completed.

The current candidate hashes match the promotion receipt, both current files
pass the base and connectivity validators, the copy and live reports name the
same 26 intents, and the database postflight is green. The graph was therefore
not automatically restored from backup: an uncoordinated rollback of a shared
database after a completed pass would be a larger unbounded mutation. The
backup remains the deliberate repair point if later semantic review finds a
concrete defect.

This is still an attribution discontinuity, so it is not erased by green
mechanical proof.

## Repair Added

Future promotions now add three guards:

1. One filesystem lock covers candidate finalization, copy dry-run, live
   apply, and receipt writing for the whole pass.
2. Canonical candidate objects are frozen under a mode-0700 directory with a
   mode-0600 manifest before intent construction.
3. Candidate hashes are checked after the copy dry-run and again immediately
   before backup/live apply. A changed candidate produces `HOLD`.

The compactor suite is green at 83/83, including an adversarial mid-pass
rewrite test. This repair preserves exact future promotion bytes; it does not
retroactively invent the missing writer identity for DGP-0042.
