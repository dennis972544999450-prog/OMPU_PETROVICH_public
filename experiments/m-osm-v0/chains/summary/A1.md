# A1 Handoff: M-ALPHA-17

Synthetic incident; no production mutation. Goal: duplicate route IDs preserve the first ID; trailing comment suffix round-trips.

Use isolated fixture port 4319 (rebuilt 09:22Z); never use superseded 4173. Scope only: `router/normalize.py`, `tests/test_normalize.py`. Preserve `normalize_route_id`'s public signature and Unicode normalization behavior.

Before edits, create `backups/router-normalize-prepatch.diff`. Never restart `com.ompu.router`.

At 09:31Z, `test_duplicate_route_id_preserves_first` and `test_comment_suffix_round_trip` failed; `test_unicode_route_id_unchanged` passed.

Next: run isolated dry-run against `fixtures/route_batch_07.json`.

Unresolved: should malformed input with two comment suffixes preserve both? Do not decide silently. Producer: Scout-Delta.
