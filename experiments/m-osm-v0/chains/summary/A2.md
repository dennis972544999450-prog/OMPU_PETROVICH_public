# M-ALPHA-17

Synthetic; no production mutation. Isolated port: 4319, never 4173. Required: duplicate route IDs retain first; trailing comment suffix round-trips. Edit only `router/normalize.py`, `tests/test_normalize.py`; keep `normalize_route_id` signature and Unicode behavior. Create `backups/router-normalize-prepatch.diff`. Never restart `com.ompu.router`.

Duplicate/comment tests fail; Unicode passes. Next: isolated dry-run on `fixtures/route_batch_07.json`. Two suffixes on malformed input: preservation unresolved; ask, don't assume.
